# Vaka V8.1.12

Telegram-бот для гильдии Free Fire с сохранением существующей системы активности, SQLite-базы, модерации, коинов, турниров, достижений, КВ и панелей.

## Запуск

1. Создай виртуальное окружение.
2. Установи зависимости: `python -m pip install -r requirements.txt`.
3. Скопируй `.env.example` в `.env`.
4. Заполни `BOT_TOKEN`, `FF_API_KEY` и `AI_API_KEY` при необходимости.
5. Запусти `python bot.py`.

## SiamBhau Premium API

Основной Free Fire API: `https://siambhau69.eu.cc`.

- `FF_API_PROVIDER=siambhau`
- `FF_API_BASE=https://siambhau69.eu.cc`
- `FF_REGION=BD`
- Индийские профили автоматически повторяются с регионом `IND`, если запрос `BD` не дал профиль.
- API-ключ хранится только в `.env`.

`/ff UID` получает полный профиль через `/freefireinfo/bhau`, затем пытается получить BR/CS статистику и Ban Check.

Дополнительные команды:

- `/ffstats UID [br|cs]`
- `/guildinfo [CLAN_ID]`
- `/bancheck UID`
- `/ffapi`

## Панели

### Гостевая

- 📝 Регистрация
- 📩 Запрос в ги
- ⚔️ Назначить КВ
- ℹ️ О НАС
- ❓ ПОМОЩЬ

### Участник

Профиль, рейтинг, активность, коины, участники, прогресс, турниры, достижения, серия, коин-топ, Free Fire, КВ, магазин, заявка, рефералы, правила и помощь.

### Администратор

Игроки, профили/API, активность, недели, публикация, полная статистика, награды, логи, турниры, антинакрутка, КВ, заявки, созыв, модерация, роли, администрация и резервные копии.

## КВ

Предложение КВ проходит FSM-форму. Администратор ранга 4+ должен одобрить предложение. После одобрения Vaka публикует в `GUILD_CHAT_ID` название гильдии противника, составы, дату/время и назначение.

## Активность

- Данные мониторинга принимаются только из `MONITORING_CHAT_ID`.
- В течение недели изменяется недельное значение.
- В понедельник в 04:10 МСК закрывается предыдущая неделя.
- Rollover защищён от повторного начисления.
- Lifetime-активность не изменяется обычным обновлением API-профиля.

## База данных

Рабочая БД: `data/guild_activity.db`.

Перед выпуском V8.0.0 была восстановлена из предоставленного пользователем DB + WAL и checkpoint'нута. В релизе сохранены игроки, недели, недельная активность, роли, логи, достижения, коины, награды и остальные существующие таблицы.

## Проверка релиза

Офлайн-проверка:

```text
python tools/verify_release.py
```

Тесты:

```text
python -m pytest -q
```

Синтаксис:

```text
python -m py_compile bot.py config.py database.py ff_api.py parser.py scheduler.py
```

## Исключённые Iris-модули

По заданию владельца Vaka не реализует модули: Дуэли, Кубы, Кланы, Кружки, Отношения, Репутация, Награды, Закладки, Заметки, Таймеры, Каталог, Ирис-биржа, Inline-режим, Business Bot, Репорты, Розыгрыши, Telegram-интеграцию и Чёрный список Iris.


## V2.8.0
Сборка поверх Vaka V2.7: единая административная навигация, команды админов, KV history/scheduled, 30-минутные предупреждения, 5-минутное удаление сообщений бота, 12-часовая очистка мусора и локальные ответы Вака. Старые функции и RP/18+ RP сохранены.
